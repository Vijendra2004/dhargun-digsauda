import 'package:flutter/material.dart';

class Commonclass extends StatelessWidget {
  const Commonclass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
