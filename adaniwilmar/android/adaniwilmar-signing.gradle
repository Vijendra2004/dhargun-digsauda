android {
    signingConfigs {
        release {
            storeFile file(System.getenv('KEYSTORE_FILE') ?: 'C:/Users/ramesh.rathinavel/adaniNew.jks')
            storePassword System.getenv('KEYSTORE_PASSWORD') ?: 'adaniapp'
            keyAlias System.getenv('KEY_ALIAS') ?: 'adaniapp'
            keyPassword System.getenv('KEY_PASSWORD') ?: 'adaniapp'
        }
        debug {
            storeFile file(System.getenv('KEYSTORE_FILE') ?: 'C:/Users/ramesh.rathinavel/adaniNew.jks')
            storePassword System.getenv('KEYSTORE_PASSWORD') ?: 'adaniapp'
            keyAlias System.getenv('KEY_ALIAS') ?: 'adaniapp'
            keyPassword System.getenv('KEY_PASSWORD') ?: 'adaniapp'
        }
    }
}